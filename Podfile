# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

def pods
    # Pods for App
    pod 'Alamofire'
    pod 'SwiftKeychainWrapper'
    pod 'PromisesSwift'
    
end

target 'App' do
    use_frameworks!
    pods
end

target 'Layout' do
    use_frameworks!
    pods
end
